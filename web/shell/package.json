{
  "name": "@liveboard/shell",
  "version": "0.1.0",
  "type": "module",
  "files": ["dist-npm"],
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "build:npm": "LIVEBOARD_BASE=./ bunx --bun vite build --outDir dist-npm",
    "typecheck": "tsc --noEmit"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
